// Foodie: Supabase config. Copy from config.example.js and add your keys for database mode.
// Leave empty to use localStorage (offline demo).
// TEMP: Supabase disabled to test loading - restore keys when fixed
window.FOODIE_CONFIG = window.FOODIE_CONFIG || {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: ''
};
